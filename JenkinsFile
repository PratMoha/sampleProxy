pipeline {
  agent any
  stages {

    stage('Checkout'){
      checkout scm
    }
    
    stage('Build APIProxy Bundle') {
    
      steps {
          // send build started notifications
       
       

      zip dir: 'apiproxy', glob: '', zipFile: 'nameOfFile'
      }
    }

    
  }
 
}