pipeline {
  agent any
  stages {
    
    stage('Build APIProxy Bundle') {
    
      steps {
          // send build started notifications
       
        sh '''#!/bin/bash

  zip -r CI_CD_PROXY apiproxy/'''
      }
    }

    
  }
 
}