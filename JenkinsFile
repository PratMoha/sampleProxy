pipeline {
  agent any
  stages {
    
    stage('Build APIProxy Bundle') {
    
      steps {
          // send build started notifications
       
       

      zip archive: true, dir: 'apiproxy', glob: '', zipFile: 'nameOfFile'
      }
    }

    
  }
 
}